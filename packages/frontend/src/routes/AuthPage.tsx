import { AuthPage as AuthPageComponent } from '../components/auth/AuthPage.js';

/**
 * Combined authentication page route
 * Provides both sign up and login functionality in a split-screen design
 */
export function AuthPage() {
  return <AuthPageComponent />;
}
